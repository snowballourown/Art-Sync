package com.artsync.controller;

/**
 * @deprecated SpaceSlotController 로 대체되었습니다.
 *             이 파일은 삭제해도 됩니다.
 *             참가자용 슬롯 조회: GET /api/spaces/{spaceId}/slots?date=YYYY-MM-DD
 */
// @RestController  ← 비활성화: SpaceSlotController 사용
public class SlotController {
}
